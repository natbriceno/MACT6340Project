<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= project.title %> | Nat Briceno</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

    <%- include('partials/navbar') %>


    <section class="py-5">
        <div class="container">
          <div class="row align-items-center">
            <!-- Left side: Text -->
            <div class="col-md-6">
              <h1><%= project.title %></h1>
              <p class="lead"><%= project.description %></p>
      
              <div class="mt-4">
                <h5>Project Details</h5>
                <ul class="list-unstyled">
                  <li><strong>Price:</strong> <%= project.price %></li>
                  <li><strong>Status:</strong> <%= project.status %></li>
                  <li><strong>Quantity:</strong> <%= project.quantity %></li>
                  <li><strong>Royalties:</strong> <%= project.royalties %></li>
                  <li>
                    <strong>Contract Address:</strong> 
                    <span id="contractAddress"><%= project.contract_address %></span>
                    <button onclick="copyContract()" class="btn btn-sm btn-outline-secondary ms-2">Copy</button>
                  </li>
                </ul>
              </div>
              <a href="/" class="btn btn-secondary mt-3">Back to Home</a>
            </div>
      
            <!-- Right side: Image -->
            <div class="col-md-6 text-center">
              <img src="<%= project.image_url %>" class="img-fluid rounded shadow-sm" alt="<%= project.title %>">
            </div>
          </div>
        </div>
      </section>
      

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!--Index JavaScript Reference - Mainly for MetaMask wallet connection-->
    <script type = "module" src="./js/index.js"></script>
</body>
</html>


